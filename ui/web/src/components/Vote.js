import React, { Component } from 'react';

class Vote extends Component {
  render() {
    return (
      //query with userid and linkid to see if user has voted and change arrow accordingly
      //mutation on click
      <div className="up-arrow"></div>
    );
  }
}

export default Vote;
